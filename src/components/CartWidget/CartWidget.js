import cart from "./assets/carrito.svg";

const CartWidget = () => {
  return (
    <div>
      <img src={cart} alt="cart-widget" width={15} height={15} />0
    </div>
  );
};

export default CartWidget;
